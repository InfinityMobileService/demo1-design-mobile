{
  "name": "MBH Customer App — static mockup",
  "ports": [
    { "name": "web", "port": 8080 }
  ],
  "terminals": [
    {
      "name": "static-server",
      "command": "python3 -m http.server 8080 --bind 0.0.0.0",
      "description": "Serves the static HTML mockup over HTTP at http://localhost:8080 (entry point: index.html). Pure static site — no build or dependencies."
    }
  ]
}
